import React  from 'react';
import ReactDOM from 'react-dom';
import Example from './memoAndCallback'

ReactDOM.render(
  <Example />,
  document.getElementById('root')
);


//基础的hook有：useState、useEffect、useContext

// 额外的hook有：useReducer

